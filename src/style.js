import styled from 'styled-components';

export const Title = styled.div`
  width: 18%;
  font-size: 32px;
  font-weight: 600;
  color: #19234b;
  font-family: Poppins;
  position: relative;
  animation: fadeBlue 5s;
  animation-iteration-count: infinite;
  transform-origin: '22%';
`;

export const WordOrange = styled.span`
  display: inline;
  font-size: 32px;
  font-weight: 600;
  font-family: Poppins;
  color: #f8811a;
  /* animation: fadeBlue 5s;
animation-iteration-count: infinite; */
  transform-origin: '22%';
`;
